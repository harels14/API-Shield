services:
  ai-service:
    build: ./ai-service
    ports:
      - "8000:8000"

  spring-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      AI_SERVICE_URL: http://ai-service:8000
    depends_on:
      - ai-service
